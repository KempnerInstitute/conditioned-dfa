# Targeted follow-ups, frozen before execution — 18 September 2026

The user authorized implementation of the external-comments assessment. This is a new phase, distinct from the completed September 15 allocation. The machine-readable plan is `configs/ndfa_revision_followups_20260918.json`; it pins the source files and every case. Maximum requested allocation is 220 GPU-minutes across three one-H100 jobs on `kempner_h100_priority` (work 80, width 80, stability 60 minutes). No automatic reruns, replacement seeds or extension of this allocation are authorized by this plan. A numerical failure remains an outcome; an infrastructure or implementation failure is recorded separately and reviewed before any repair.

All studies reuse the established CIFAR-10 split, training-only input standardization, two-view augmentation, 64 sampled identities per update, and manual MLP. Test data are not a new untouched benchmark. Models are evaluated at the specified final endpoint; there is no checkpoint selection. Each completed final model receives one saved official-test prediction record with independently calculated float64 cross-entropy. Development cases use validation only.

- **Longer measured work:** BP, DFA, A-nDFA and FD-DFA at 60 and 120 seconds of synchronized update work; five new paired seeds, 40 cases. Transfer the previously selected FD-study settings without retuning and stretch the existing warmup/cosine schedule over each budget. This tests transfer across budgets, not a separately optimized convergence envelope. The work clock includes sampling, augmentation, gradients, conditioning, optimizer updates and synchronization; validation/export are excluded equally.
- **Width-specific error damping:** widths 1024–512 and 2048–1024, 48,000 training examples, 5,000 SGD updates, learning rate 0.1, BN and feedback scale 0.1. Select full and diagonal error damping independently from 3, 30, 300 and 3000 using the mean final validation CE over two development seeds (32 cases). Require both runs finite; ties favor the largest damping. Freeze that choice before five-seed DFA/A/E/diagonal-E/K confirmation at both widths (50 cases). A uses relative activity damping 30; K combines A with E's selected damping. No K outcome selects damping. Record actual error spectra, effective rank, damped condition number and update angles at fixed checkpoints. This is an equal-grid direct error-side ablation and a damping-transfer follow-up.
- **Stability intervention:** DFA/A/E/K, BN off/on, feedback scale 0.1/1, five paired seeds (80 cases). All other settings fixed: 48,000 examples, width 1024–512, 5,000 SGD updates, learning rate 0.1 and relative A/E damping 30. Record validation logit scales, loss quantiles, loss-tail contributions and sparse layerwise update/weight norms. The intervention tests the joint role of BN and feedback scale under one recipe; it does not establish that those two changes explain all differences between earlier protocols.

Use paired effect sizes and all seed endpoints, conditional on the fixed dataset/split. Any additional contrasts are labeled follow-up analyses rather than retroactively added to earlier confirmatory families. Do not use these outcomes to alter the original reported experiments.
